THE TOOL YOU WILL BUILD: fieldviz-mini

A tiny library for visualizing 2D dynamical fields, attractors, and flow lines using a compact, clean API.

Think of it as:

“A simple attractor / vector-field visualizer for exploring dynamical systems.”

Everyone understands vector fields.
Nothing controversial.
Nothing too big.
But: extremely impressive for a single person to ship cleanly.

This is the best “artifact breadcrumb” because it’s both humble and technically credible.

⸻

🏗️ FULL REPLIT BUILD SPEC

Copy–paste EVERYTHING below into Replit as your “instructions” prompt.

This will construct:
	•	fieldviz_mini/ Python package
	•	A working vector-field visualizer
	•	Streamline integrator
	•	Quiver plotting
	•	A simple attractor demo
	•	Docs
	•	README.md
	•	LICENSE
	•	Tests
	•	Setup for PyPI
	•	Example notebook (optional)

⸻

📦 DIRECTORY STRUCTURE

fieldviz-mini/
│
├── README.md
├── pyproject.toml
├── setup.cfg
├── LICENSE
│
├── fieldviz_mini/
│   ├── __init__.py
│   ├── fields.py
│   ├── integrators.py
│   ├── visualize.py
│   ├── presets.py
│
├── examples/
│   ├── simple_attractor.py
│   ├── demo.ipynb
│
└── tests/
    ├── test_integrator.py
    ├── test_fields.py

This is the exact structure required for a PyPI-ready package.

⸻

📄 FILE CONTENTS (copy–paste directly)

pyproject.toml

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fieldviz-mini"
version = "0.1.0"
description = "Tiny vector-field and attractor visualizer"
readme = "README.md"
requires-python = ">=3.9"
authors = [
  { name="Ryan Sabouhi", email="example@example.com" }
]
license = { file="LICENSE" }
keywords = ["dynamics", "vector fields", "visualization", "attractors", "complex systems"]
dependencies = [
  "numpy",
  "matplotlib"
]

[project.urls]
Homepage = "https://github.com/YOUR_GITHUB/fieldviz-mini"


⸻

setup.cfg

[options]
package_dir=
    =.
packages = find:

[options.packages.find]
where = .


⸻

LICENSE

Use MIT:

MIT License

Copyright (c) 2025

Permission is hereby granted...


⸻

📁 fieldviz_mini/ core files

__init__.py

from .fields import VectorField
from .integrators import euler_step, rk4_step, integrate_streamline
from .visualize import plot_vector_field, plot_streamlines
from .presets import lorenz_field, spiral_sink, saddle_point


⸻

fields.py

import numpy as np

class VectorField:
    def __init__(self, func):
        """
        func: callable (x, y) -> (dx, dy)
        """
        self.func = func

    def evaluate(self, X, Y):
        FX = np.zeros_like(X)
        FY = np.zeros_like(Y)
        for i in range(X.shape[0]):
            for j in range(X.shape[1]):
                fx, fy = self.func(X[i, j], Y[i, j])
                FX[i, j] = fx
                FY[i, j] = fy
        return FX, FY


⸻

integrators.py

import numpy as np

def euler_step(field, x, y, dt=0.01):
    dx, dy = field.func(x, y)
    return x + dt*dx, y + dt*dy

def rk4_step(field, x, y, dt=0.01):
    f = field.func
    k1 = f(x, y)
    k2 = f(x + dt*k1[0]/2, y + dt*k1[1]/2)
    k3 = f(x + dt*k2[0]/2, y + dt*k2[1]/2)
    k4 = f(x + dt*k3[0],   y + dt*k3[1])
    dx = (k1[0] + 2*k2[0] + 2*k3[0] + k4[0]) / 6
    dy = (k1[1] + 2*k2[1] + 2*k3[1] + k4[1]) / 6
    return x + dt*dx, y + dt*dy

def integrate_streamline(field, x0, y0, steps=1000, dt=0.01, method="rk4"):
    x, y = x0, y0
    traj = [(x, y)]
    step_func = rk4_step if method=="rk4" else euler_step

    for _ in range(steps):
        x, y = step_func(field, x, y, dt=dt)
        traj.append((x, y))
    return np.array(traj)


⸻

visualize.py

import numpy as np
import matplotlib.pyplot as plt

def plot_vector_field(field, xlim=(-2,2), ylim=(-2,2), density=20):
    x = np.linspace(*xlim, density)
    y = np.linspace(*ylim, density)
    X, Y = np.meshgrid(x, y)
    FX, FY = field.evaluate(X, Y)

    plt.figure(figsize=(6,6))
    plt.quiver(X, Y, FX, FY, color="black", alpha=0.6)
    plt.title("Vector Field")
    plt.xlabel("x")
    plt.ylabel("y")
    plt.axis("equal")
    plt.show()

def plot_streamlines(field, seeds, steps=500, dt=0.01):
    from .integrators import integrate_streamline

    plt.figure(figsize=(6,6))
    for (x0, y0) in seeds:
        traj = integrate_streamline(field, x0, y0, steps=steps, dt=dt)
        plt.plot(traj[:,0], traj[:,1], linewidth=1)

    plt.title("Streamlines")
    plt.axis("equal")
    plt.show()


⸻

presets.py

import numpy as np
from .fields import VectorField

def spiral_sink(a=0.5, b=1.0):
    def f(x, y):
        return -a*x - b*y, b*x - a*y
    return VectorField(f)

def saddle_point():
    def f(x, y):
        return x, -y
    return VectorField(f)

def lorenz_field(sigma=10, beta=8/3, rho=28):
    def f(x, y):
        # 2D projection (not full Lorenz), simple toy
        dx = sigma * (y - x)
        dy = x * (rho - y) - x
        return dx, dy
    return VectorField(f)


⸻

📄 README.md (clean, simple)

# fieldviz-mini

A tiny Python library for exploring **2D vector fields**, **attractors**, and **flow dynamics**.

### Features
- Simple API for defining vector fields  
- RK4 & Euler integrators  
- Streamline plotting  
- Quiver field visualization  
- A few preset fields (spiral sink, saddle, Lorenz-like)

### Install

pip install fieldviz-mini

### Example
```python
from fieldviz_mini import spiral_sink, plot_vector_field, plot_streamlines

field = spiral_sink()
plot_vector_field(field)
plot_streamlines(field, seeds=[(1,1), (-1,0), (0.5,-1)])


⸻

This package is intentionally minimal — a small tool for intuition-building and exploration.

---

# 📄 Example script: `examples/simple_attractor.py`

```python
from fieldviz_mini import spiral_sink, plot_vector_field, plot_streamlines

field = spiral_sink()
plot_vector_field(field)
plot_streamlines(field, seeds=[(1,1), (-1,0), (0.5,-1)])


⸻

🧪 Tests (tests/test_fields.py)

from fieldviz_mini import spiral_sink

def test_eval():
    field = spiral_sink()
    fx, fy = field.func(1.0, -1.0)
    assert isinstance(fx, float)
    assert isinstance(fy, float)


⸻

🧪 Tests (tests/test_integrator.py)

from fieldviz_mini import spiral_sink
from fieldviz_mini.integrators import integrate_streamline

def test_streamline_runs():
    field = spiral_sink()
    traj = integrate_streamline(field, 1.0, 1.0, steps=10)
    assert traj.shape == (11, 2)